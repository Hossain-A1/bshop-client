import PlaceOrder from "@/components/PlaceOrder";
import { metadata } from "../layout";

metadata.title = "B-shop - Place-order";

const PlaceOrderPage = () => {
  return <PlaceOrder />;
};

export default PlaceOrderPage;
