import Profile from "@/components/Profile";
import { metadata } from "../layout";
metadata.title = "B-shop - user - profile";
const ProfilePage = () => {
  return <Profile />;
};

export default ProfilePage;
